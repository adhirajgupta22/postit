<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

    <div class="w-full min-h-screen bg-zinc-900 p-6 text-white">
        <div class="w-full flex justify-end">
            <a class="bg-red-600 rounded-md text-sm inline-block px-3 py-2 outline-none" href="/logout">Logout</a>
        </div>
        <h2 class="text-3xl">Edit your post</h2>
        <form method="post" action="/update/<%= post._id%>">
            <textarea class="mt-5 p-2 w-1/3 block resize-none border-2 border-zinc-800 rounded-md  outline-none bg-transparent" placeholder="what's on your mind ?"  name="content"><%= post.content%></textarea>
            <input class="px-3 py-2 text-sm w-40 bg-yellow-500 text-black  block rounded-md mt-2" type="submit" value="Update post">
        </form>
    </div>
    
</body>
</html>